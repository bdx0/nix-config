# nix-config

## Run this nix-env

```bash
nix run github:bdx0/nix-config --extra-experimental-features 'nix-command flakes'
nix develop github:bdx0/nix-config --extra-experimental-features 'nix-command flakes'
```
